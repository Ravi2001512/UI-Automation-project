# This directory holds one CSV file per database table that needs to be
# seeded before the Class-Examination test suite runs.
#
# File naming convention:
#   <exact_table_name>.csv
#
# Files are processed in alphabetical order, so prefix with numbers when
# foreign-key dependencies exist, e.g.:
#   01_classes.csv
#   02_examinations.csv
#
# Add your seed CSVs alongside this file.
